name: Build and Test

runs:
  using: composite
  steps:
    - name: Execute Tests
      shell: bash
      run: |
        bazelisk test //... 

    - name: Ensure everything is tidy
      shell: bash
      run: |
        bazelisk run //:tidy_check
